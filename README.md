# taichi
Taichi is a plugin-driven test orchestration framework for Go and web projects — run API, UI, static, and regression tests with a single CLI, extend via third-party skill plugins, integrate with AI agents for auto-fix loops, and ship JUnit/JSON/HTML reports.
